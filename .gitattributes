# Normalise line endings to LF in the repository and on checkout, on every
# platform. Prettier is configured with `endOfLine: "lf"`, so without this a
# Windows clone (autocrlf=true by default) would fail `npm run format:check`
# on every file while CI stayed green.
* text=auto eol=lf
